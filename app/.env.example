# 本地/构建环境变量示例 —— 复制为 app/.env 后按需修改(不提交到版本库)
#
# 只要不改这里, 打包产物里的接口地址就是同源的 '/api':
#   - 线上: nginx 把 /api 反代到后端(默认, 什么都不用配)
#   - 本地开发: vite dev server 代理到 http://127.0.0.1:3000
#
# 把打包产物放到**别的域名/机器**上时才需要填(只写域名即可, 会自动补 /api):
# VITE_API_BASE=https://app.jiacheng.cyou
#
# 同时后端必须放开跨域, 否则浏览器会拦掉请求(见 server/.env 的 CORS_ORIGINS):
#   CORS_ORIGINS=https://你的前端域名
#
# 本地开发时 /api 的代理目标(默认本机 3000; 想让本地前端直读线上数据再改):
# VITE_API_TARGET=https://app.jiacheng.cyou
